version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: Test
          commands:
            - checkout
            - pip install pytest
            - mkdir pytest_project
            - cd pytest_project
            - apt install python3.8-venv
